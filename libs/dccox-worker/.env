# DC-Cox Worker Configuration

# Port to listen on (default: 8001)
DCCOX_WORKER_PORT=8001

# URL of the master node (default: http://localhost:8000)
DCCOX_MASTER_URL=http://localhost:8000

# Default worker name shown in the UI (default: my-worker)
DCCOX_WORKER_NAME=my-worker
